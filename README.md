# C++ Networking Example (TCP)

This project demonstrates a simple TCP client-server model in C++.

## 🚀 How to Run

### Linux / macOS
```bash
g++ src/server.cpp -o server
g++ src/client.cpp -o client
./server
# In another terminal:
./client
